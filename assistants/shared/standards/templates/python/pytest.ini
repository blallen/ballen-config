[pytest]
addopts = -ra
testpaths = tests
xfail_strict = true
markers =
    integration: tests that cross a process, service, or storage boundary
    slow: tests excluded from the normal fast feedback loop
