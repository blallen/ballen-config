# Publication Receipt v1

`publication-receipt/v1` stores minimal per-item outcomes for one guarded
publication attempt. It may contain bounded reasons and provider IDs or URLs,
but never credentials, request headers, raw provider responses, or complete
payloads.
