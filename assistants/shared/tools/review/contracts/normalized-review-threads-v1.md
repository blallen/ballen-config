# Normalized Review Threads v1

`normalized-review-threads/v1` is a provider-neutral, read-only snapshot of
native review threads. It retains provider/change identity, current head,
native thread and comment IDs, state, chronology, required text, logical
location, and bounded normalization limitations.

It is evidence for response planning, not permission to edit, commit, push,
reply, or resolve a thread.
